@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="container d-flex justify-content-center">
    <div class="card shadow rounded-1 w-50 mt-3">
        <div class="card-body bg-light text-center">
            <p><mark style="background-color: cornsilk">IOptionsMonitor</mark> OnChange basic example.</p>
            <p>See console logs for change notifications</p>
        </div>
    </div>
</div>
